//  aca dejamos los prototipos de las funciones y su documentacion
#include <stdlib.h>
//#include "biblioteca.h"

/** \brief Que hace la funcion: en este caso, pide un numero entero y lo retorna
 *
 * \param que recibe la funcion, en este caso, un array de char char[]. Luego se pone que representa para el usuario:  mensaje para el usuario
 * \return int el numero ingresado.
 *
 */


//int pedirEntero (char[]);

//int incrementarValor(int);

void incrementarValor(int*);
